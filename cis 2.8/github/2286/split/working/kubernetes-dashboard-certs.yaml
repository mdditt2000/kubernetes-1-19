apiVersion: cis.f5.com/v1
kind: TLSProfile
metadata:
  labels:
    f5cr: "true"
  name: kubernetes-dashboard-certs
  namespace: default
spec:
  hosts:
    - f5demo.com
  tls:
    clientSSL: /Common/clientssl
    reference: bigip
    termination: edge
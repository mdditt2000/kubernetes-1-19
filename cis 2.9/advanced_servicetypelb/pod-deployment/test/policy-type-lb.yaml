apiVersion: cis.f5.com/v1
kind: Policy
metadata:
  labels:
    f5cr: "true"
  name: type-lb
  namespace: default
spec:
  profiles:
    tcp: /Common/Custom_TCP
    persistenceProfile: none
  snat:
    /Common/self-ip
